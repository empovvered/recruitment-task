//INFO: Pagination crosses component and URL boundaries 1-based, because that is what a reader means by "page 1"
export const DEFAULT_PAGE_INDEX = 1

export const DEFAULT_PAGE_SIZE = 25

export const PAGE_SIZES = [25, 50, 100]
